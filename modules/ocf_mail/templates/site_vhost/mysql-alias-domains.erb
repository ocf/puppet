hosts = mysql
user = ocfmail-ro
password = <%= @mysql_ro_password %>
dbname = ocfmail
query = SELECT domain FROM domains WHERE domain = '%s'

# sanity check on number of domains returned (should be only one per query)
expansion_limit = 1
