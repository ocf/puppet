DocumentRoot /services/http/ocf/www
<Directory /services/http/ocf/www/>
    DirectoryIndex index.shtml index.html
    Options IncludesNoExec Indexes FollowSymlinks

    # Allow caching of shtml pages
    SSILastModified on

    # Add missing tilde for user dirs
    RewriteEngine On
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^([a-z]{3,8}(/.*)?)$ /~$1 [R=301]

    # Disable .htaccess files
    AllowOverride None
</Directory>

ScriptAlias /cgi-bin/ /services/http/ocf/cgi-bin/
Alias /staff_hours /services/http/ocf/cgi-bin/staff_hours.cgi
<Directory /services/http/ocf/cgi-bin>
    Options Includes ExecCGI FollowSymlinks

    # Disable .htaccess files for performance
    AllowOverride None
</Directory>

LogLevel warn
ErrorLog ${APACHE_LOG_DIR}/error.log
