# Allow authenticated nodes to access contrib share
[contrib]
  path /opt/puppet/contrib
  allow *

# Allow authenticated nodes to access private host share
[private]
  path /opt/puppet/private/%h
  allow *
